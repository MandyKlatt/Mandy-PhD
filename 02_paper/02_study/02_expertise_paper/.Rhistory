df_aoi_avg_long <- df_aoi_avg_long %>%
mutate(AOI = factor(AOI, levels = c(
"Average_duration_all", "Students", "Anna", "Bianca", "Carl(a)", "Disruptive_Person",
"Board_Screen", "Material_Teacher", "Material_Students",
"Classroom_Others", "Nametag_Anna", "Nametag_Bianca", "Nametag_Carl(a)"
))) %>%
arrange(AOI)
# Summarize descriptives for average duration of fixations, including "Average_duration_all"
aois_descriptives_durations <- df_aoi_avg_long %>%
group_by(Group, AOI) %>%
summarise(
N = n(),
M = round(mean(Average_duration, na.rm = TRUE), 2),
SD = round(sd(Average_duration, na.rm = TRUE), 2),
Min = round(min(Average_duration, na.rm = TRUE), 2),
Max = round(max(Average_duration, na.rm = TRUE), 2)
)
# Display summarized descriptives in a table, including "Average_duration_all"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
# Filter and select necessary columns
df_aoi_filtered <- df_aoi %>%
filter(TOI == "Lesson") %>%
select(
Group,
Participant,
Duration_of_interval,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations")
)
# Create a new AOI 'Students' by summing durations and counts for Anna, Bianca, and Carl(a)
df_aoi_filtered <- df_aoi_filtered %>%
mutate(
Total_duration_of_fixations.Students =
`Total_duration_of_fixations.Anna` +
`Total_duration_of_fixations.Bianca` +
`Total_duration_of_fixations.Carl(a)`,
Number_of_fixations.Students =
`Number_of_fixations.Anna` +
`Number_of_fixations.Bianca` +
`Number_of_fixations.Carl(a)`
)
# Calculate average fixation duration for each AOI individually (including "Disruptive_Person")
df_aoi_avg_long <- df_aoi_filtered %>%
pivot_longer(
cols = starts_with("Total_duration_of_fixations") | starts_with("Number_of_fixations"),
names_to = "Measure_AOI",
values_to = "Values"
) %>%
separate(Measure_AOI, into = c("Measure", "AOI"), sep = "\\.", extra = "merge") %>%
pivot_wider(
names_from = Measure,
values_from = Values
) %>%
mutate(
Average_duration = round(Total_duration_of_fixations / Number_of_fixations, digits = 2)
)
# Calculate Average_duration_all as the summarized value across all AOIs, excluding "Disruptive_Person"
average_duration_all <- df_aoi_filtered %>%
rowwise() %>%
mutate(
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`,
Average_duration_all = ifelse(Total_fixations_excl_disruptive > 0,
round(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, digits = 2),
NA)
) %>%
select(Group, Average_duration_all) %>%
distinct() %>%
mutate(AOI = "Average_duration_all")
# Add the overall average duration as a new row to the long-format data
df_aoi_avg_long <- bind_rows(df_aoi_avg_long, average_duration_all)
# Set the order of AOIs based on the specified sequence, including "Average_duration_all"
df_aoi_avg_long <- df_aoi_avg_long %>%
mutate(AOI = factor(AOI, levels = c(
"Average_duration_all", "Students", "Anna", "Bianca", "Carl(a)", "Disruptive_Person",
"Board_Screen", "Material_Teacher", "Material_Students",
"Classroom_Others", "Nametag_Anna", "Nametag_Bianca", "Nametag_Carl(a)"
))) %>%
arrange(AOI)
# Summarize descriptives for average duration of fixations, including "Average_duration_all"
aois_descriptives_durations <- df_aoi_avg_long %>%
group_by(Group, AOI) %>%
summarise(
N = n(),
M = round(mean(Average_duration, na.rm = TRUE), 2),
SD = round(sd(Average_duration, na.rm = TRUE), 2),
Min = round(min(Average_duration, na.rm = TRUE), 2),
Max = round(max(Average_duration, na.rm = TRUE), 2)
)
# Visualization: Boxplot for average duration of fixations across AOIs
ggplot(df_aoi_avg_long, aes(x = AOI, y = Average_duration, fill = Group)) +
geom_boxplot(outlier.shape = NA) +
geom_jitter(
position = position_jitterdodge(jitter.width = 0.15),
size = 1.5,
alpha = 0.7
) +
scale_fill_brewer(palette = "RdBu") +
labs(x = "AOI", y = "Average Duration of Fixations (ms)") +
ggtitle("Average Duration of Fixations across all AOIs") +
theme_minimal() +
theme(legend.position = "top", axis.text.x = element_text(angle = 45, hjust = 1))
# Display summarized descriptives in a table, including "Average_duration_all"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
# Filter and select necessary columns
df_aoi_filtered <- df_aoi %>%
filter(TOI == "Lesson") %>%
select(
Group,
Participant,
Duration_of_interval,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations")
)
# Create a new AOI 'Students' by summing durations and counts for Anna, Bianca, and Carl(a)
df_aoi_filtered <- df_aoi_filtered %>%
mutate(
Total_duration_of_fixations.Students =
`Total_duration_of_fixations.Anna` +
`Total_duration_of_fixations.Bianca` +
`Total_duration_of_fixations.Carl(a)`,
Number_of_fixations.Students =
`Number_of_fixations.Anna` +
`Number_of_fixations.Bianca` +
`Number_of_fixations.Carl(a)`
)
# Calculate average fixation duration for each AOI individually (including "Disruptive_Person")
df_aoi_avg_long <- df_aoi_filtered %>%
pivot_longer(
cols = starts_with("Total_duration_of_fixations") | starts_with("Number_of_fixations"),
names_to = "Measure_AOI",
values_to = "Values"
) %>%
separate(Measure_AOI, into = c("Measure", "AOI"), sep = "\\.", extra = "merge") %>%
pivot_wider(
names_from = Measure,
values_from = Values
) %>%
mutate(
Average_duration = round(Total_duration_of_fixations / Number_of_fixations, digits = 2)
)
# Calculate Average_duration_all as the summarized value across all AOIs, excluding "Disruptive_Person"
average_duration_all <- df_aoi_filtered %>%
rowwise() %>%
mutate(
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`,
Average_duration_all = ifelse(Total_fixations_excl_disruptive > 0,
round(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, digits = 2),
NA)
) %>%
select(Group, Average_duration_all) %>%
distinct() %>%
mutate(AOI = "Average_duration_all")
# Add the overall average duration as a new row to the long-format data
df_aoi_avg_long <- bind_rows(df_aoi_avg_long, average_duration_all)
# Set the order of AOIs based on the specified sequence, including "Average_duration_all"
df_aoi_avg_long <- df_aoi_avg_long %>%
mutate(AOI = factor(AOI, levels = c(
"Average_duration_all", "Students", "Anna", "Bianca", "Carl(a)", "Disruptive_Person",
"Board_Screen", "Material_Teacher", "Material_Students",
"Classroom_Others", "Nametag_Anna", "Nametag_Bianca", "Nametag_Carl(a)"
))) %>%
arrange(AOI)
# Summarize descriptives for average duration of fixations, including "Average_duration_all"
aois_descriptives_durations <- df_aoi_avg_long %>%
group_by(Group, AOI) %>%
summarise(
N = n(),
M = round(mean(Average_duration, na.rm = TRUE), 2),
SD = round(sd(Average_duration, na.rm = TRUE), 2),
Min = round(min(Average_duration, na.rm = TRUE), 2),
Max = round(max(Average_duration, na.rm = TRUE), 2)
)
# Visualization: Boxplot for average duration of fixations across AOIs
ggplot(df_aoi_avg_long, aes(x = AOI, y = Average_duration, fill = Group)) +
geom_boxplot(outlier.shape = NA) +
geom_jitter(
position = position_jitterdodge(jitter.width = 0.15),
size = 1.5,
alpha = 0.7
) +
scale_fill_brewer(palette = "RdBu") +
labs(x = "AOI", y = "Average Duration of Fixations (ms)") +
ggtitle("Average Duration of Fixations across all AOIs") +
theme_minimal() +
theme(legend.position = "top", axis.text.x = element_text(angle = 45, hjust = 1))
# Display summarized descriptives in a table, including "Average_duration_all"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
# Filter and select necessary columns
df_aoi_filtered <- df_aoi %>%
filter(TOI == "Lesson") %>%
select(
Group,
Participant,
Duration_of_interval,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations")
)
# Create a new AOI 'Students' by summing durations and counts for Anna, Bianca, and Carl(a)
df_aoi_filtered <- df_aoi_filtered %>%
mutate(
Total_duration_of_fixations.Students =
`Total_duration_of_fixations.Anna` +
`Total_duration_of_fixations.Bianca` +
`Total_duration_of_fixations.Carl(a)`,
Number_of_fixations.Students =
`Number_of_fixations.Anna` +
`Number_of_fixations.Bianca` +
`Number_of_fixations.Carl(a)`
)
# Calculate average fixation duration for each AOI individually (including "Disruptive_Person")
df_aoi_avg_long <- df_aoi_filtered %>%
pivot_longer(
cols = starts_with("Total_duration_of_fixations") | starts_with("Number_of_fixations"),
names_to = "Measure_AOI",
values_to = "Values"
) %>%
separate(Measure_AOI, into = c("Measure", "AOI"), sep = "\\.", extra = "merge") %>%
pivot_wider(
names_from = Measure,
values_from = Values
) %>%
mutate(
Average_duration = round(Total_duration_of_fixations / Number_of_fixations, digits = 2)
)
# Calculate Average_duration_all as the mean of average durations across all AOIs, excluding "Disruptive_Person"
average_duration_all <- df_aoi_avg_long %>%
filter(AOI != "Disruptive_Person") %>%
group_by(Group) %>%
summarise(Average_duration_all = round(mean(Average_duration, na.rm = TRUE), 2)) %>%
mutate(AOI = "Average_duration_all")
# Add the overall average duration as a new row to the long-format data
df_aoi_avg_long <- bind_rows(df_aoi_avg_long, average_duration_all)
# Display summarized descriptives in a table, including "Average_duration_all"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
# Filter und Auswahl der relevanten Spalten
df_aoi_filtered <- df_aoi %>%
filter(TOI == "Lesson") %>%
select(
Group,
Participant,
Duration_of_interval,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations")
)
# Erstelle eine neue AOI "Students" durch Summierung der Fixationsdauer und -anzahl für Anna, Bianca und Carl(a)
df_aoi_filtered <- df_aoi_filtered %>%
mutate(
Total_duration_of_fixations.Students =
`Total_duration_of_fixations.Anna` +
`Total_duration_of_fixations.Bianca` +
`Total_duration_of_fixations.Carl(a)`,
Number_of_fixations.Students =
`Number_of_fixations.Anna` +
`Number_of_fixations.Bianca` +
`Number_of_fixations.Carl(a)`
)
# Berechne die durchschnittliche Fixationsdauer für jede AOI individuell (inkl. "Disruptive_Person")
df_aoi_avg_long <- df_aoi_filtered %>%
pivot_longer(
cols = starts_with("Total_duration_of_fixations") | starts_with("Number_of_fixations"),
names_to = "Measure_AOI",
values_to = "Values"
) %>%
separate(Measure_AOI, into = c("Measure", "AOI"), sep = "\\.", extra = "merge") %>%
pivot_wider(
names_from = Measure,
values_from = Values
) %>%
mutate(
Average_duration = round(Total_duration_of_fixations / Number_of_fixations, digits = 2)
)
# Berechne `Average_duration_all` als Durchschnittsdauer über alle AOIs, ohne die "Disruptive_Person"
average_duration_all <- df_aoi_filtered %>%
rowwise() %>%
mutate(
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`,
Average_duration_all = ifelse(Total_fixations_excl_disruptive > 0,
round(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, digits = 2),
NA)
) %>%
select(Group, Average_duration_all) %>%
distinct() %>%
mutate(AOI = "Average_duration_all")
# Füge `Average_duration_all` als neue Zeile in die Langformat-Daten ein
df_aoi_avg_long <- bind_rows(df_aoi_avg_long, average_duration_all)
# Setze die AOIs in der gewünschten Reihenfolge, inkl. "Average_duration_all"
df_aoi_avg_long <- df_aoi_avg_long %>%
mutate(AOI = factor(AOI, levels = c(
"Average_duration_all", "Students", "Anna", "Bianca", "Carl(a)", "Disruptive_Person",
"Board_Screen", "Material_Teacher", "Material_Students",
"Classroom_Others", "Nametag_Anna", "Nametag_Bianca", "Nametag_Carl(a)"
))) %>%
arrange(AOI)
# Zusammenfassung der deskriptiven Statistiken, inkl. "Average_duration_all"
aois_descriptives_durations <- df_aoi_avg_long %>%
group_by(Group, AOI) %>%
summarise(
N = n(),
M = round(mean(Average_duration, na.rm = TRUE), 2),
SD = round(sd(Average_duration, na.rm = TRUE), 2),
Min = round(min(Average_duration, na.rm = TRUE), 2),
Max = round(max(Average_duration, na.rm = TRUE), 2)
)
# Visualisierung: Boxplot der durchschnittlichen Fixationsdauer für alle AOIs
ggplot(df_aoi_avg_long, aes(x = AOI, y = Average_duration, fill = Group)) +
geom_boxplot(outlier.shape = NA) +
geom_jitter(
position = position_jitterdodge(jitter.width = 0.15),
size = 1.5,
alpha = 0.7
) +
scale_fill_brewer(palette = "RdBu") +
labs(x = "AOI", y = "Average Duration of Fixations (ms)") +
ggtitle("Average Duration of Fixations across all AOIs") +
theme_minimal() +
theme(legend.position = "top", axis.text.x = element_text(angle = 45, hjust = 1))
# Anzeige der zusammengefassten deskriptiven Statistiken in einer Tabelle, inkl. "Average_duration_all"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
average_duration_all <- df_aoi_filtered %>%
rowwise() %>%
mutate(
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`,
Average_duration_all = ifelse(Total_fixations_excl_disruptive > 0,
round(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, digits = 2),
NA)
) %>%
select(Group, Average_duration_all) %>%
distinct() %>%
mutate(AOI = "Average_duration_all")
View(average_duration_all)
# Füge `Average_duration_all` als neue Zeile in die Langformat-Daten ein
df_aoi_avg_long <- bind_rows(df_aoi_avg_long, average_duration_all)
# Setze die AOIs in der gewünschten Reihenfolge, inkl. "Average_duration_all"
df_aoi_avg_long <- df_aoi_avg_long %>%
mutate(AOI = factor(AOI, levels = c(
"Average_duration_all", "Students", "Anna", "Bianca", "Carl(a)", "Disruptive_Person",
"Board_Screen", "Material_Teacher", "Material_Students",
"Classroom_Others", "Nametag_Anna", "Nametag_Bianca", "Nametag_Carl(a)"
))) %>%
arrange(AOI)
# Zusammenfassung der deskriptiven Statistiken, inkl. "Average_duration_all"
aois_descriptives_durations <- df_aoi_avg_long %>%
group_by(Group, AOI) %>%
summarise(
N = n(),
M = round(mean(Average_duration, na.rm = TRUE), 2),
SD = round(sd(Average_duration, na.rm = TRUE), 2),
Min = round(min(Average_duration, na.rm = TRUE), 2),
Max = round(max(Average_duration, na.rm = TRUE), 2)
)
# Anzeige der zusammengefassten deskriptiven Statistiken in einer Tabelle, inkl. "Average_duration_all"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
# Filtere und wähle die relevanten Spalten aus
df_aoi_filtered <- df_aoi %>%
filter(TOI == "Lesson") %>%
select(
Group,
Participant,
Duration_of_interval,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations")
)
# Erstelle die AOI "Students" durch Summieren der Werte für Anna, Bianca und Carl(a)
df_aoi_filtered <- df_aoi_filtered %>%
mutate(
Total_duration_of_fixations.Students =
`Total_duration_of_fixations.Anna` +
`Total_duration_of_fixations.Bianca` +
`Total_duration_of_fixations.Carl(a)`,
Number_of_fixations.Students =
`Number_of_fixations.Anna` +
`Number_of_fixations.Bianca` +
`Number_of_fixations.Carl(a)`
)
# Berechne die durchschnittliche Fixationsdauer für jede AOI individuell (einschließlich "Disruptive_Person")
df_aoi_avg_long <- df_aoi_filtered %>%
pivot_longer(
cols = starts_with("Total_duration_of_fixations") | starts_with("Number_of_fixations"),
names_to = "Measure_AOI",
values_to = "Values"
) %>%
separate(Measure_AOI, into = c("Measure", "AOI"), sep = "\\.", extra = "merge") %>%
pivot_wider(
names_from = Measure,
values_from = Values
) %>%
mutate(
Average_duration = round(Total_duration_of_fixations / Number_of_fixations, digits = 2)
)
# Berechne `Average_duration_mtu` und `Number_fixation_mtu` durch Aufsummieren der Fixationsdauer und -anzahl, ohne "Disruptive_Person"
df_mtu_summary <- df_aoi_filtered %>%
summarise(
Group = unique(Group),
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`
) %>%
mutate(
Average_duration_mtu = round(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, digits = 2),
Number_fixation_mtu = Total_fixations_excl_disruptive,
AOI = "MTU"
) %>%
select(Group, AOI, Average_duration_mtu, Number_fixation_mtu)
# Berechne `Average_duration_mtu` und `Number_fixation_mtu` durch Aufsummieren der Fixationsdauer und -anzahl, ohne "Disruptive_Person"
df_mtu_summary <- df_aoi_filtered %>%
summarise(
Group = unique(Group),
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`
) %>%
mutate(
Average_duration_mtu = round(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, digits = 2),
Number_fixation_mtu = Total_fixations_excl_disruptive,
AOI = "MTU"
) %>%
select(Group, AOI, Average_duration_mtu, Number_fixation_mtu)
# Filtere und wähle die relevanten Spalten aus
df_aoi_filtered <- df_aoi %>%
filter(TOI == "Lesson") %>%
select(
Group,
Participant,
Duration_of_interval,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations")
)
# Erstelle die AOI "Students" durch Summieren der Werte für Anna, Bianca und Carl(a)
df_aoi_filtered <- df_aoi_filtered %>%
mutate(
Total_duration_of_fixations.Students =
`Total_duration_of_fixations.Anna` +
`Total_duration_of_fixations.Bianca` +
`Total_duration_of_fixations.Carl(a)`,
Number_of_fixations.Students =
`Number_of_fixations.Anna` +
`Number_of_fixations.Bianca` +
`Number_of_fixations.Carl(a)`
)
# Berechne die durchschnittliche Fixationsdauer für jede AOI individuell (einschließlich "Disruptive_Person")
df_aoi_avg_long <- df_aoi_filtered %>%
pivot_longer(
cols = starts_with("Total_duration_of_fixations") | starts_with("Number_of_fixations"),
names_to = "Measure_AOI",
values_to = "Values"
) %>%
separate(Measure_AOI, into = c("Measure", "AOI"), sep = "\\.", extra = "merge") %>%
pivot_wider(
names_from = Measure,
values_from = Values
) %>%
mutate(
Average_duration = round(Total_duration_of_fixations / Number_of_fixations, digits = 2)
)
# Berechne `Average_duration_mtu` und `Number_fixation_mtu` durch Aufsummieren der Fixationsdauer und -anzahl, ohne "Disruptive_Person"
df_mtu_summary <- df_aoi_filtered %>%
rowwise() %>%  # Zeilenweise Berechnung
mutate(
Total_duration_excl_disruptive = sum(c_across(starts_with("Total_duration_of_fixations")), na.rm = TRUE) - `Total_duration_of_fixations.Disruptive_Person`,
Total_fixations_excl_disruptive = sum(c_across(starts_with("Number_of_fixations")), na.rm = TRUE) - `Number_of_fixations.Disruptive_Person`
) %>%
ungroup() %>%
summarise(
Group = unique(Group),
Average_duration_mtu = round(mean(Total_duration_excl_disruptive / Total_fixations_excl_disruptive, na.rm = TRUE), 2),
Number_fixation_mtu = sum(Total_fixations_excl_disruptive, na.rm = TRUE)
) %>%
mutate(AOI = "MTU")
# Füge `Average_duration_mtu` und `Number_fixation_mtu` zur Langformat-Datenstruktur hinzu
df_aoi_avg_long <- bind_rows(df_aoi_avg_long, df_mtu_summary)
# Setze die AOIs in der gewünschten Reihenfolge, einschließlich "MTU"
df_aoi_avg_long <- df_aoi_avg_long %>%
mutate(AOI = factor(AOI, levels = c(
"MTU", "Students", "Anna", "Bianca", "Carl(a)", "Disruptive_Person",
"Board_Screen", "Material_Teacher", "Material_Students",
"Classroom_Others", "Nametag_Anna", "Nametag_Bianca", "Nametag_Carl(a)"
))) %>%
arrange(AOI)
# Berechne deskriptive Statistiken, einschließlich "MTU"
aois_descriptives_durations <- df_aoi_avg_long %>%
group_by(Group, AOI) %>%
summarise(
N = n(),
M = round(mean(Average_duration, na.rm = TRUE), 2),
SD = round(sd(Average_duration, na.rm = TRUE), 2),
Min = round(min(Average_duration, na.rm = TRUE), 2),
Max = round(max(Average_duration, na.rm = TRUE), 2)
)
# Visualisierung: Boxplot der durchschnittlichen Fixationsdauer für alle AOIs
ggplot(df_aoi_avg_long, aes(x = AOI, y = Average_duration, fill = Group)) +
geom_boxplot(outlier.shape = NA) +
geom_jitter(
position = position_jitterdodge(jitter.width = 0.15),
size = 1.5,
alpha = 0.7
) +
scale_fill_brewer(palette = "RdBu") +
labs(x = "AOI", y = "Average Duration of Fixations (ms)") +
ggtitle("Average Duration of Fixations across all AOIs") +
theme_minimal() +
theme(legend.position = "top", axis.text.x = element_text(angle = 45, hjust = 1))
# Ausgabe der deskriptiven Tabelle, einschließlich "MTU"
knitr::kable(aois_descriptives_durations,
caption = "N, M, SD, min & max duration of fixations in milliseconds (across all AOIs)")
