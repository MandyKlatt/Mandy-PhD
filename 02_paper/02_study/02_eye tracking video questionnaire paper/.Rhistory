)
knitr::kable(nof.table,
caption = "N, M, SD, min & max number of fixation (micro-teaching unit)")
# filter group & total duration & number fixation on AOI Disruptive person
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select(Group,
Participant,
"Number_of_fixations.Disruptive_Person") %>%
mutate(Group = as_factor(Group)) -> df_aoi_disrup
# N, M, SD, min & max number of fixation on AOI students
nof.disrup.table <-
df_aoi_disrup %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Number_of_fixations.Disruptive_Person), digits = 2),
"SD" = round(sd(Number_of_fixations.Disruptive_Person), digits = 2),
"Min" = round(min(Number_of_fixations.Disruptive_Person), digits = 2),
"Max" = round(max(Number_of_fixations.Disruptive_Person), digits = 2)
)
knitr::kable(nof.disrup.table,
caption = "N, M, SD, min & max number of fixation on AOI disruptive person (micro-teaching unit)")
# exclude invalid participants
df_aoi <- df_aoi %>%
filter(!Participant == 223 | 232)
# remove temporary data for a cleaner workspace
rm(list = ls(pattern = "^tib_ProVisio"))
rm(work_data)
rm(file_names)
rm(i)
XXX 223 & 232 sind fehlerhaft --> Daten nochmal komplett neu ziehen!! XXX
#### 2.2.1 Number of fixation (micro-teaching unit)
```{r nof_all, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
# filter group, participants, total duration & number fixation (micro-teaching unit)
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select (
Group,
Participant,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations"),
starts_with("Average_duration"),
!"Total_duration_of_fixations.Disruptive_Person",
!"Number_of_fixations.Disruptive_Person",
!"Average_duration_of_fixations.Disruptive_Person"
) %>%
rowwise() %>%
transmute(
Group = Group,
Participant = Participant,
Sum_duration_fixation = sum(c_across(starts_with("Total_duration")), na.rm = TRUE),
Sum_average_duration_fixation = sum(c_across(starts_with("Average_duration")), na.rm = TRUE),
Sum_number_fixation = sum(c_across(starts_with("Number_of")), na.rm = TRUE)
) %>%
mutate(
Average_duration_calc = round(Sum_duration_fixation / Sum_number_fixation, digits = 0),
Group = as_factor(Group)
) %>%
drop_na() -> df_aoi_sum
# N, M, SD, min & max number of fixation (micro-teaching unit)
nof.table <-
df_aoi_sum %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Sum_number_fixation), digits = 2),
"SD" = round(sd(Sum_number_fixation), digits = 2),
"Min" = round(min(Sum_number_fixation), digits = 2),
"Max" = round(max(Sum_number_fixation), digits = 2)
)
knitr::kable(nof.table,
caption = "N, M, SD, min & max number of fixation (micro-teaching unit)")
# filter group & total duration & number fixation on AOI "disruptive person"
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select(Group,
Participant,
"Number_of_fixations.Disruptive_Person") %>%
mutate(Group = as_factor(Group)) -> df_aoi_disrup
# N, M, SD, min & max number of fixation on AOI "disruptive person"
nof.disrup.table <-
df_aoi_disrup %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Number_of_fixations.Disruptive_Person), digits = 2),
"SD" = round(sd(Number_of_fixations.Disruptive_Person), digits = 2),
"Min" = round(min(Number_of_fixations.Disruptive_Person), digits = 2),
"Max" = round(max(Number_of_fixations.Disruptive_Person), digits = 2)
)
knitr::kable(nof.disrup.table,
caption = "N, M, SD, min & max number of fixation on AOI disruptive person (during disruption)")
# filter group & total duration & number fixation on AOI "disruptive person"
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select(Group,
Participant,
"Number_of_fixations.Disruptive_Person") %>%
mutate(Group = as_factor(Group)) -> df_aoi_disrup
# exclude invalid participants
df_aoi <- df_aoi %>%
filter(!Participant == 223 | 232)
# exclude invalid participants
df_aoi <- df_aoi %>%
filter(!Participant == 223 & 232)
# exclude invalid participants
df_aoi <- df_aoi %>%
filter(!Participant == 223 + 232)
# exclude invalid participants
df_aoi <- df_aoi %>%
filter(!Participant == 223,
!Participant == 232)
# filter group, participants, total duration & number fixation (micro-teaching unit)
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select (
Group,
Participant,
starts_with("Total_duration_of_fixations"),
starts_with("Number_of_fixations"),
starts_with("Average_duration"),
!"Total_duration_of_fixations.Disruptive_Person",
!"Number_of_fixations.Disruptive_Person",
!"Average_duration_of_fixations.Disruptive_Person"
) %>%
rowwise() %>%
transmute(
Group = Group,
Participant = Participant,
Sum_duration_fixation = sum(c_across(starts_with("Total_duration")), na.rm = TRUE),
Sum_average_duration_fixation = sum(c_across(starts_with("Average_duration")), na.rm = TRUE),
Sum_number_fixation = sum(c_across(starts_with("Number_of")), na.rm = TRUE)
) %>%
mutate(
Average_duration_calc = round(Sum_duration_fixation / Sum_number_fixation, digits = 0),
Group = as_factor(Group)
) %>%
drop_na() -> df_aoi_sum
# N, M, SD, min & max number of fixation (micro-teaching unit)
nof.table <-
df_aoi_sum %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Sum_number_fixation), digits = 2),
"SD" = round(sd(Sum_number_fixation), digits = 2),
"Min" = round(min(Sum_number_fixation), digits = 2),
"Max" = round(max(Sum_number_fixation), digits = 2)
)
knitr::kable(nof.table,
caption = "N, M, SD, min & max number of fixation (micro-teaching unit)")
# plotting number of fixations on ALL AOIS
df_aoi_sum %>%
ggplot(mapping = aes(x = Group, y = Sum_number_fixation)) +
geom_boxplot(mapping = aes(fill = Group), outlier.shape = NA) +
geom_point(
size = 2,
alpha = 0.7,
position = position_jitter(
seed = 1,
width = 0.1,
height = 0.1
)
) +
ylim(0, 2500) +
labs(x = "", y = "Number of fixation") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Number of fixation\n(micro-teaching unit)") +
theme_cowplot() +
theme(legend.position = "none") -> plot_number_group
plot_number_group
#### t-test & effect size "Number of fixation in entire micro-teaching unit"
```{r nof_all_ttest, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
# NUMBER OF FIXATIONS on all AOIs
# t-test for expertise differences
t.test(
x = df_aoi_sum$Sum_number_fixation[df_aoi_sum$Group == "Expert"],
y = df_aoi_sum$Sum_number_fixation[df_aoi_sum$Group == "Novice"],
var.equal = TRUE
)
# NUMBER OF FIXATIONS on all AOIs
# effect size for expertise differences
d_number_all <- CohenD(
x = df_aoi_sum$Sum_number_fixation[df_aoi_sum$Group == "Expert"],
y = df_aoi_sum$Sum_number_fixation[df_aoi_sum$Group == "Novice"],
na.rm = TRUE
)
round(d_number_all, 2)
#### 2.2.2 Number of fixations on AOI "students" (micro-teaching unit)
```{r nof_students, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
# filter group & total duration & number fixation on AOI Students
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select (
Group,
Participant,
"Total_duration_of_fixations.Anna",
"Total_duration_of_fixations.Bianca",
"Total_duration_of_fixations.Carl(a)",
"Number_of_fixations.Anna",
"Number_of_fixations.Bianca",
"Number_of_fixations.Carl(a)"
) %>%
rowwise() %>%
transmute(
Group = Group,
Participant = Participant,
Stud_duration_fixation = sum(c_across(starts_with("Total_duration")), na.rm = TRUE),
Stud_number_fixation = sum(c_across(starts_with("Number_of")), na.rm = TRUE)
) %>%
mutate(
Average_duration_stud = round(Stud_duration_fixation / Stud_number_fixation, digits = 0),
Group = as_factor(Group)
) -> df_aoi_stud
# N, M, SD, min & max number of fixation on AOI students
nof.stud.table <-
df_aoi_stud %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Stud_number_fixation), digits = 2),
"SD" = round(sd(Stud_number_fixation), digits = 2),
"Min" = round(min(Stud_number_fixation), digits = 2),
"Max" = round(max(Stud_number_fixation), digits = 2)
)
knitr::kable(nof.stud.table,
caption = "N, M, SD, min & max number of fixation on AOI students (micro-teaching unit)")
# plotting average duration on AOI Students
df_aoi_stud %>%
ggplot(mapping = aes(x = Group, y = Stud_number_fixation)) +
geom_boxplot(mapping = aes(fill = Group), outlier.shape = NA) +
geom_point(
size = 2,
alpha = 0.7,
position = position_jitter(
seed = 1,
width = 0.1,
height = 0.1
)
) +
labs(x = "", y = "Number of fixation") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Number of fixation on AOI `Students`\n(micro-teaching unit)") +
theme_cowplot() +
theme(legend.position = "none") -> plot_number_stud
plot_number_stud
#### t-test & effect size "Number of fixation on AOI students"
```{r nof_stud_ttest, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
# NUMBER OF FIXATIONS on AOI students
# t-test for expertise differences
t.test(x = df_aoi_stud$Stud_number_fixation[df_aoi_stud$Group == "Expert"],
y = df_aoi_stud$Stud_number_fixation[df_aoi_stud$Group == "Novice"],
var.equal = TRUE)
# NUMBER OF FIXATIONS on AOI students
# effect size for expertise differences
d_number_stud <- CohenD(x = df_aoi_stud$Stud_number_fixation[df_aoi_stud$Group == "Expert"],
y = df_aoi_stud$Stud_number_fixation[df_aoi_stud$Group == "Novice"],
na.rm = TRUE)
round(d_number_stud, 2)
# filter group & total duration & number fixation on AOI "disruptive person"
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select(Group,
Participant,
"Number_of_fixations.Disruptive_Person") %>%
mutate(Group = as_factor(Group)) -> df_aoi_disrup
# N, M, SD, min & max number of fixation on AOI "disruptive person"
nof.disrup.table <-
df_aoi_disrup %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Number_of_fixations.Disruptive_Person), digits = 2),
"SD" = round(sd(Number_of_fixations.Disruptive_Person), digits = 2),
"Min" = round(min(Number_of_fixations.Disruptive_Person), digits = 2),
"Max" = round(max(Number_of_fixations.Disruptive_Person), digits = 2)
)
knitr::kable(nof.disrup.table,
caption = "N, M, SD, min & max number of fixation on AOI disruptive person (during disruption)")
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group,
y = Number_of_fixations.Disruptive_Person)) +
geom_boxplot(mapping = aes(fill = Group),
outlier.shape = NA) +
geom_point(size = 2,
alpha = 0.7,
position = position_jitter(seed = 1,
width = 0.1,
height = 0.1)) +
# scale_x_discrete(labels = paste0(c("Novice\n","Expert\n"),
#                                  "M = ", c(mean_dur_nov, mean_dur_exp),
#                                  "; SD = ", c(sd_dur_nov, sd_dur_exp)
#                                  )
#                  ) +
labs(x ="",
y = "Anzahl Fixationen") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Anzahl Fixationen AOI\n`Störperson`") +
scale_x_discrete(labels=c("Novice" = "Unerfahren",
"Expert" = "Erfahren")) +
theme_cowplot() +
theme(legend.position="none",
axis.text.x = element_text(size = 34),
axis.text.y = element_text(size = 18),
axis.title = element_text(size = 34),
plot.title = element_text(size = 35,
face = "bold")
) -> plot_number_disrup
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group, y = Number_of_fixations.Disruptive_Person)) +
geom_boxplot(mapping = aes(fill = Group), outlier.shape = NA) +
geom_point(
size = 2,
alpha = 0.7,
position = position_jitter(
seed = 1,
width = 0.1,
height = 0.1
)
) +
labs(x = "", y = "Number of fixation") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Number of fixation AOI\n`disruptive person`") +
theme_cowplot() +
theme(legend.position = "none") -> plot_number_disrup
plot_number_disrup
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group, y = Number_of_fixations.Disruptive_Person)) +
geom_boxplot(mapping = aes(fill = Group), outlier.shape = NA) +
geom_point(
size = 2,
alpha = 0.7,
position = position_jitter(
seed = 1,
width = 0.1,
height = 0.1
)
) +
ylim(0, 140)
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group, y = Number_of_fixations.Disruptive_Person)) +
geom_boxplot(mapping = aes(fill = Group), outlier.shape = NA) +
geom_point(
size = 2,
alpha = 0.7,
position = position_jitter(
seed = 1,
width = 0.1,
height = 0.1
)
) +
ylim(0, 140) +
labs(x = "", y = "Number of fixation") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Number of fixation AOI\n`disruptive person`") +
theme_cowplot() +
theme(legend.position = "none") -> plot_number_disrup
plot_number_disrup
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group, y = Number_of_fixations.Disruptive_Person)) +
geom_boxplot(mapping = aes(fill = Group), outlier.shape = NA) +
geom_point(
size = 2,
alpha = 0.7,
position = position_jitter(
seed = 1,
width = 0.1,
height = 0.1
)
) +
ylim(0, 150) +
labs(x = "", y = "Number of fixation") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Number of fixation AOI\n`disruptive person`") +
theme_cowplot() +
theme(legend.position = "none") -> plot_number_disrup
plot_number_disrup
# NUMBER OF FIXATIONS on AOI disruptive person
# t-test for expertise differences
t.test(x = df_aoi_disrup$Number_of_fixations.Disruptive_Person[df_aoi_disrup$Group == "Expert"],
y = df_aoi_disrup$Number_of_fixations.Disruptive_Person[df_aoi_disrup$Group == "Novice"],
var.equal = TRUE)
# NUMBER OF FIXATIONS
# effect size for expertise differences
d_number_disrup <- CohenD(x = df_aoi_disrup$Number_of_fixations.Disruptive_Person[df_aoi_disrup$Group == "Expert"],
y = df_aoi_disrup$Number_of_fixations.Disruptive_Person[df_aoi_disrup$Group == "Novice"],
na.rm = TRUE)
round(d_number_disrup, 2)
# NUMBER OF FIXATIONS on AOI disruptive person
# t-test for expertise differences
t.test(x = df_aoi_disrup$Number_of_fixations.Disruptive_Person[df_aoi_disrup$Group == "Expert"],
y = df_aoi_disrup$Number_of_fixations.Disruptive_Person[df_aoi_disrup$Group == "Novice"],
var.equal = TRUE)
# filter group & total duration & number fixation on AOI Disruptive person
df_aoi %>%
filter(TOI == "Lesson") %>%
dplyr::select (
Group,
"Total_duration_of_fixations.Disruptive_Person",
"Number_of_fixations.Disruptive_Person"
) %>%
rowwise() %>%
mutate(
Average_duration_disrup = round(
Total_duration_of_fixations.Disruptive_Person / Number_of_fixations.Disruptive_Person,
digits = 0
),
Group = as_factor(Group)
) -> df_aoi_disrup
# N, M, SD, min & max number of fixation on AOI "disruptive person"
dur.disrup.table <-
df_aoi_disrup %>%
group_by(Group) %>%
summarise(
N = n(),
"M" = round(mean(Average_duration_disrup), digits = 2),
"SD" = round(sd(Average_duration_disrup), digits = 2),
"Min" = round(min(Average_duration_disrup), digits = 2),
"Max" = round(max(Average_duration_disrup), digits = 2)
)
knitr::kable(dur.disrup.table,
caption = "N, M, SD, min & max average duration of fixation on AOI disruptive person (during disruption)")
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group,
y = Average_duration_disrup)) +
geom_boxplot(mapping = aes(fill = Group),
outlier.shape = NA) +
geom_point(size = 2,
alpha = 0.7,
position = position_jitter(seed = 1,
width = 0.1,
height = 0.1)) +
labs(x ="",
y = "Milliseconds") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Average duration of fixation AOI\n`disruptive person`") +
theme_cowplot() +
theme(legend.position="none") -> plot_average_duration_disrup
plot_average_duration_disrup
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group,
y = Average_duration_disrup)) +
geom_boxplot(mapping = aes(fill = Group),
outlier.shape = NA) +
geom_point(size = 2,
alpha = 0.7,
position = position_jitter(seed = 1,
width = 0.1,
height = 0.1)) +
labs(x ="",
y = "Milliseconds") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Average duration of fixation\nAOI`disruptive person`") +
theme_cowplot() +
theme(legend.position="none") -> plot_average_duration_disrup
plot_average_duration_disrup
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group,
y = Average_duration_disrup)) +
geom_boxplot(mapping = aes(fill = Group),
outlier.shape = NA) +
geom_point(size = 2,
alpha = 0.7,
position = position_jitter(seed = 1,
width = 0.1,
height = 0.1)) +
labs(x ="",
y = "Milliseconds") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Average duration of fixation AOI\n`disruptive person`") +
theme_cowplot() +
theme(legend.position="none") -> plot_average_duration_disrup
plot_average_duration_disrup
# plotting average duration on AOI Disruptive person
df_aoi_disrup %>%
ggplot(mapping = aes(x = Group,
y = Average_duration_disrup)) +
geom_boxplot(mapping = aes(fill = Group),
outlier.shape = NA) +
geom_point(size = 2,
alpha = 0.7,
position = position_jitter(seed = 1,
width = 0.1,
height = 0.1)) +
ylim(0, 1500) +
labs(x ="",
y = "Milliseconds") +
scale_fill_brewer(palette  = "RdBu") +
ggtitle("Average duration of fixation AOI\n`disruptive person`") +
theme_cowplot() +
theme(legend.position="none") -> plot_average_duration_disrup
plot_average_duration_disrup
# AVERAGDE DURATION OF FIXATIONS on AOI disruptive person
# t-test for expertise differences
t.test(x = df_aoi_disrup$Average_duration_disrup[df_aoi_disrup$Group == "Expert"],
y = df_aoi_disrup$Average_duration_disrup[df_aoi_disrup$Group == "Novice"],
var.equal = TRUE)
# NUMBER OF FIXATIONS
# effect size for expertise differences
d_dur_disrup <- CohenD(x = df_aoi_disrup$Average_duration_disrup[df_aoi_disrup$Group == "Expert"],
y = df_aoi_disrup$Average_duration_disrup[df_aoi_disrup$Group == "Novice"],
na.rm = TRUE)
round(d_dur_disrup, 2)
